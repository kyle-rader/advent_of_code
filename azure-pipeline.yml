variables:
  ruby_version: 2.6
  python_version: 3.8

jobs:
- job: Ruby
  strategy:
    parallel: true
  pool:
    vmImage: 'ubuntu-16.04'
  steps:
  - task: UseRubyVersion@0
    inputs:
      versionSpec: '>= $(ruby_version)'
  - task: CmdLine@2
    inputs:
      script: |
        echo Starting Ruby Tests

- job: Python
  strategy:
    parallel: true
  pool:
    vmImage: 'ubuntu-16.04'
  steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '$(python_version)'
      addToPath: true
      architecture: 'x64'
  - task: CmdLine@2
    inputs:
      script: |
        echo Starting Python Tests